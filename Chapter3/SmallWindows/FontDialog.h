namespace SmallWindows {
  bool FontDialog(Window* windowPtr, Font& font);
};

/*
namespace SmallWindows {
//  bool FontDialog(Window* windowPtr, Font& font, Color& color);
  bool FontDialog(Window* windowPtr, Font& font);
};

  class FontDialog {
    public:
      FontDialog(Window* windowPtr, Font font);
      FontDialog(Window* windowPtr, Font font, Color color);
      bool Display();
      Font GetFont();
      Color FontColor();

    private:
      CHOOSEFONT chooseFont;
  };*/